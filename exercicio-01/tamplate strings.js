// Exemplos
const postTitle = 'Teste de qualidade'
const postAuthor = 'Wiliam Cruz'
const postComments = 13

// Concatenação das informações
const postMessage = '2 post"' + postTitle + '", do' + postAuthor + ', tem ' + postComments + 'coment'

console.log(postMessage)